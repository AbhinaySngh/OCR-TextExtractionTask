To run this project, following steps are required-

--> Install Tesseract OCR for Windows and note the install path (e.g., C:\Program Files\Tesseract-OCR\tesseract.exe).

--> Install all dependencies
>> pip install -r requirements.txt

--> To run the project type command in your terminal
>> python app.py